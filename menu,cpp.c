#include <iostream>
#include <iomanip>
#include "menu.h"

void showCustomerMenu(std::map<int, MenuItem>& inventory) {
    // Customer menu implementation
    while (true) {
        // Display customer menu options and handle user input
    }
}

void showInventoryMenu(std::map<int, MenuItem>& inventory) {
    // Inventory menu implementation
    while (true) {
        // Display inventory menu options and handle user input
    }
}

void loginMenu(std::map<int, MenuItem>& inventory, void (*nextMenu)(std::map<int, MenuItem>&)) {
    // Login menu implementation
    while (true) {
        // Display login menu options and handle user input
    }
}

